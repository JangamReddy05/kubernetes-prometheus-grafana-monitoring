apiVersion: v1
kind: Service
metadata:
  name: monitoring-app
  namespace: monitoring
  labels:
    app: monitoring-app
spec:
  selector:
    app: monitoring-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP