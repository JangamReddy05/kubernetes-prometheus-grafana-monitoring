apiVersion: v1
kind: Service
metadata:
  name: monitoring-demo-app
  namespace: monitoring-app
  labels:
    app: monitoring-demo-app
spec:
  selector:
    app: monitoring-demo-app
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: ClusterIP